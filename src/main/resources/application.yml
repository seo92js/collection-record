spring:
  h2:
    console:
      enabled: true
  datasource:
    #url: jdbc:h2:tcp://localhost/~/collection-record
    url: jdbc:h2:mem:test
    username: sa
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
